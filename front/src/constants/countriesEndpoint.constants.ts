export const countriesSchemaResponse: { countries: string[] } = {
  countries: ["string", "string"],
};

export const countriesExampleResponse: { countries: string[] } = {
  countries: ["England", "Scotland", "Germany"],
};
